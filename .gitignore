data/
node_modules/
# Every dotenv variant, not just .env/.env.local: `next build` also loads
# .env.production and .env.production.local, and a NEXT_PUBLIC_* value in one of
# those gets inlined into the client bundle. .env.example is the one exception.
.env*
!.env.example
.DS_Store
.next/
*.tsbuildinfo

/app/generated/prisma
